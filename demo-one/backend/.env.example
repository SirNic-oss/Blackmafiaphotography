# PostgreSQL connection for Prisma (optional — API falls back to seed data if unavailable)
DATABASE_URL=postgresql://user:password@localhost:5432/demo_one

PORT=5000
FRONTEND_URL=http://localhost:3000
